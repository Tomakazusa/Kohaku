walk <- 0
for (i in 1:100) {
  step <- sample(c(-1, 1), size = 1)
  walk <- c(walk, walk[i] + step)
}
plot(walk, type = "l")

walk<-0
loc<-0
while(loc>-3&loc<3){
  step<-sample(c(-1,1),size=1)
  loc<-loc+step
  walk<-c(walk,loc)
}
plot(walk,type='l')
